import styled from "styled-components";

export const Wrapper = styled.div`
    .title-wrapper {
        justify-content : space-between;
    }
    .search {
        float: left;
        width: 30%;
    }
`